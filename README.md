## Development
### Setup
Install cobra with `go get -u github.com/spf13/cobra/cobra`

### Adding commands
To add new commands execute `cobra add <cmdname>` in root directory

### Run CLI
Execute `go run main.go <cmdname>`
